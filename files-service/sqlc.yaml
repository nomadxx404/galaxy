version: "2"
sql:
  - schema: "internal/repository/schema/"
    queries: "internal/repository/queries/"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "internal/repository/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
        overrides:
          - db_type: "varchar"
            go_type: "string"
          - db_type: "timestamptz"
            go_type: "time.Time"
